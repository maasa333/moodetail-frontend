import React from 'react';
import Prompt from '../components/Prompt'

const Prompts = props => {
    return (
        <div>
            PromptsComponent
        </div>
    );
}

export default Prompts;
