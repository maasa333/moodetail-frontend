import React from 'react';
import Prompt from '../components/Prompt'

const Prompts = props => {
    return (
        <div>
            <Prompt />
        </div>
    );
}

export default Prompts;
