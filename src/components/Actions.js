import React from 'react';
import Action from '../components/Action'

const Actions = props => {
    console.log(props)
    return (
        <div>
            <ul>
                <Action />
            </ul>
        </div>
    );
}

export default Actions;